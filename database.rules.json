{
  "rules": {
    "admins": {
      ".read": "auth != null && root.child('admins').child(auth.uid).exists()",
      ".write": false
    },
    "rooms": {
      ".indexOn": ["meta/lastActiveAt"],
      "$roomId": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).exists()",
        "meta": {
          ".validate": "newData.hasChildren(['createdBy', 'createdAt', 'lastActiveAt'])"
        },
        "config": {
          ".validate": "newData.hasChildren(['wordCount', 'numTeams', 'skipPenalty', 'timerDuration'])"
        },
        "players": {
          "$playerId": {
            ".write": "auth != null && (auth.uid === $playerId || root.child('admins').child(auth.uid).exists())",
            ".validate": "newData.hasChildren(['name', 'color', 'teamId', 'wordsSubmitted', 'ready', 'connected', 'isAdmin'])"
          }
        },
        "teams": {
          "$teamId": {
            ".write": "auth != null && root.child('admins').child(auth.uid).exists()",
            ".validate": "newData.hasChildren(['name', 'playerOrder', 'currentPlayerIndex', 'roundScores'])"
          }
        },
        "words": {
          "$wordId": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['text', 'addedBy'])"
          }
        },
        "gameState": {
          ".write": "auth != null && root.child('admins').child(auth.uid).exists()"
        },
        "status": {
          ".write": "auth != null && root.child('admins').child(auth.uid).exists()",
          ".validate": "newData.isString() && (newData.val() === 'word-entry' || newData.val() === 'pre-start' || newData.val() === 'playing' || newData.val() === 'finished')"
        }
      }
    }
  }
}
